// src/config.js — PHATT Bridge configuration
// TODO: Replace with real config loaded from environment / .env file

const config = {
  businessUnits: [
    { name: 'Daystar' },
    { name: 'CST' },
    { name: 'Precise' },
    { name: 'Terry Sherman Law' },
  ],
};

module.exports = { config };
